{% extends 'base.html.twig' %}

{% block title %}Home page{% endblock %}

{% block body %}
<div class="container-fluid">
      
    <!-- Page Heading -->
    <div class="d-sm-flex align-items-center justify-content-between">
        <h1 class="h3 ml-5 mb-0 text-gray-800">Welcome!</h1>
    </div>
    <hr>
    <div class="container-fluid pl-5 pb-3 pt-1">
        <div class="row px-xl-5">
            <div class="col-md-6">
                <a href="" data-ad-type="horizontal">
                    <img class="img-fluid" src="img/bj2.jpg" alt="Advertising Banner">
                </a>
            </div>
            <div class="col-md-6">
                <a href="" data-ad-type="horizontal">
                    <img class="img-fluid" src="img/bj3.jpg" alt="Advertising Banner">
                </a>
            </div>
        </div>
    </div>

    <!-- Content Row -->
    {# <div class="row">

        <!-- Earnings (Monthly) Card Example -->
        <div class="col-xl-3 col-md-6 mb-4">
            {% for category in category %}
            <div class="card border-left-danger shadow h-100 py-2">
                <div class="card-body">
                    <div class="row no-gutters align-items-center">
                        <div class="col mr-2">
                            <div class="text-xs font-weight-bold text-danger text-uppercase mb-1">
                                {{ category.getName() }}</div>
                            <div class="h5 mb-0 font-weight-bold text-gray-800">$40,000</div>
                        </div>
                        <div class="col-auto">
                            <i class="" src="{{ asset('uploads/sanpham/' ~ category.getPhoto()) }}" alt="">
                        </div>
                    </div>
                </div>
            </div>
            {% endfor %}
        </div>
</div> #}
<!-- /.container-fluid -->
{% endblock %}
